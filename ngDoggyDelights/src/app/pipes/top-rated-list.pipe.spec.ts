import { TopRatedListPipe } from './top-rated-list.pipe';

describe('TopRatedListPipe', () => {
  it('create an instance', () => {
    const pipe = new TopRatedListPipe();
    expect(pipe).toBeTruthy();
  });
});
